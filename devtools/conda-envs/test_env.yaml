name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # OpenMM
  - openmm
  - pdbfixer

    # Pip-only installs
  - pip:
    - qcelemental
